[project]
name = "transformer"
version = "0.1.0"
description = "Building a Transformer from Scratch"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
	"numpy>=2.4.3",
	"torch>=2.10.0",
]
